{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Backend", // A unique label for your task
      "type": "shell", // Or "process" if you're running an executable directly
      "command": "cd outreach_backend", // The command to execute
      "presentation": {
        "reveal": "always", // Optional: Control when the terminal panel is revealed
        "panel": "new" // Optional: Open a new terminal panel for the task
      }
    }
  ]
}
